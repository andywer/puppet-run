{
  "name": "puppet-run-sample-tape",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "puppet-run ./test.js"
  },
  "devDependencies": {
    "puppet-run": "^0.3.0",
    "tape": "^4.9.1"
  }
}
