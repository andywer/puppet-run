# Basic Samples

Run the sample test:

```sh
npm install
npm run cowsay
npm run greet
npm run greet newbie
```

The npm scripts will simply run:

```sh
puppet-run ./cowsay
puppet-run ./greet
puppet-run ./greet newbie
```
